root /usr/share/nginx/html;
index index.html;
autoindex off;

location = /healthz {
    access_log off;
    default_type text/plain;
    return 200 "ok\n";
}

location /assets/ {
    add_header Cache-Control "public, max-age=300" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Lab-Server "nginx-static-http-server" always;
    try_files $uri =404;
}

location / {
    limit_except GET {
        deny all;
    }
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Lab-Server "nginx-static-http-server" always;
    try_files $uri $uri/ =404;
}

location /api/ {
    proxy_pass http://backend_app/;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Request-ID $request_id;

    proxy_hide_header X-Powered-By;

    add_header X-Lab-Proxy "nginx-reverse-proxy" always;
}

# TODO:
# Dodaj w tym pliku blok location /api/.
# Powinien przekazywac ruch do http://backend_app/.
# Przekaz naglowki Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto i X-Request-ID.
# Ukryj X-Powered-By z odpowiedzi backendu.
# Dodaj do odpowiedzi X-Lab-Proxy: nginx-reverse-proxy.
